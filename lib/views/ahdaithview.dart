import 'package:flutter/material.dart';

class Ahdaithview extends StatelessWidget {
  const Ahdaithview({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
